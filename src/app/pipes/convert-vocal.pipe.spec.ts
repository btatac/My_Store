import { ConvertVocalPipe } from './convert-vocal.pipe';

describe('ConvertVocalPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertVocalPipe();
    expect(pipe).toBeTruthy();
  });
});
